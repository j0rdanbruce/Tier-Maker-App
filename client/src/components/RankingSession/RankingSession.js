/**
 * The component that represents a ranking session that any user can join on. 
 * Users on same session can create tier lists and rank items on same session.
 */

const RankingSession = (props) => {
  
}

export default RankingSession;