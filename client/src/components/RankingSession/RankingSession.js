/**
 * The component that represents the room that users can join/create for creating tier lists and ranking them.
 */

import SortableList from "../SortableList";

const RankingSession = (props) => {

  return (
    <SortableList />
  );
}

export default RankingSession;